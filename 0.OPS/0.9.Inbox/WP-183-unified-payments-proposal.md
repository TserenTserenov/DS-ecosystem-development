# Единый учёт оплат и автоматизация допуска

> **Proposal для обсуждения с командой.**
> **Участники ревью:** Дима (Aisystant), Гиляна (бухгалтерия), Алёна (маркетинг), Юля (декан), Ильшат (менеджмент), Церен и Андрей (архитектура).
>
> **Связанные документы в этой папке:**
> - [WP-183 CRM+Billing Architecture](WP-183-crm-billing-architecture-proposal.md) -- архитектура CRM, Directus, Billing Service
> - [WP-115 Сценарий семинара](WP-115-seminar-payment-access-scenario.md) -- E2E: оплата -> допуск -> видео -> маршрутка
> - [WP-73 Архитектура платформы](WP-73-aisystant-platform-architecture.md) -- §2.9 Биллинг, каналы оплаты
> - [WP-74 Концепция использования](WP-74-platform-concept-of-use.md) -- SC-12 Подписка и оплата
> - [WP-109 Activity Hub](WP-109-activity-hub-lms-integration-proposal.md) -- Billing adapter, события оплат

---

<details open>
<summary><b>0. Принятые решения</b></summary>

### Архитектурные решения

> **Онтология:** Payment Registry (SYS.011) -- это **система** (подсистема платформы), которая принимает, записывает и отдаёт данные об оплатах. **Реестр оплат** -- это **документ** (набор записей в таблицах PostgreSQL), который Payment Registry ведёт. Система создаёт и обновляет документ; потребители (Directus, Metabase, бот Aist) читают данные из документа через систему.

| # | Решение | Обоснование |
|---|---------|-------------|
| Р1 | **Выделяем подсистему Payment Registry (SYS.011).** Payment Registry -- система учёта всех оплат. Payment Registry ведёт единый документ **Реестр оплат** как набор записей (таблицы **в Neon**) -- источник правды: кто, когда, сколько, через какой канал заплатил. Все каналы оплаты регистрируют транзакции в Реестре оплат (Neon). В переходный период (М1): Aisystant дублирует каналы 1-5 в Neon; каналы 6-8 пишут в Neon напрямую. Потребители (Directus, Metabase, бот Aist) читают из Neon | Один источник (Neon), не два. Выделение подсистемы -- первый шаг к отдельному Billing Service |
| Р2 | **Две системы: Payment Registry (SYS.011) и Billing Service (SYS.010).** Payment Registry = система учёта транзакций (уже существует де-факто, выделяем). Billing Service = новая система с бизнес-логикой (тарифы, подписки, баллы, revenue sharing, юнит-экономика). На Фазе B Payment Registry мигрирует в Billing Service как модуль | Разделение факта оплаты и бизнес-логики. Позволяет строить Billing Service поэтапно, не ломая текущий учёт |
| Р3 | **Уведомление бота: Neon → бот Aist.** Когда новая оплата появляется в Neon (от Aisystant через dual-write или от Directus), бот Aist узнаёт об этом и генерирует invite. Механизм: PG NOTIFY / Directus Flow trigger / polling. Бот Aist НЕ получает webhook от Aisystant напрямую — только через Neon | Один источник данных (Neon), бот читает оттуда. Aisystant не знает о боте |
| Р4 | **Привязка tg_id <-> email <-> aisystant_id -- через Ory (Фаза B).** Каждая система знает только свою связку с Ory (звезда), не между собой (mesh). Временный костыль до Ory не делаем | Правильная архитектура identity. Mesh = O(n^2) интеграций |
| Р5 | **Два варианта допуска в чат:** (A) бот Aist генерирует одноразовую ссылку (`createChatInviteLink`, `member_limit=1`), (B) админ создаёт одноразовую ссылку через интерфейс Telegram. Бот Aist узнаёт о вступлении в обоих случаях (`chat_member` update, поле `invite_link.creator`) | Вариант A предпочтительнее (автоматический учёт). Вариант B -- запасной (ручной допуск админом, бот Aist помечает `source: admin_invite`) |
| Р6 | **Бот МИМ (@SystemsSchool_bot) тоже принимает оплаты.** Оба бота (Aist и МИМ) -- каналы приёма оплат. Оплаты через бот МИМ записываются в Neon (Реестр оплат) так же, как оплаты через бот Aist | Бот МИМ обслуживает существующую аудиторию. Отказ от приёма оплат через него = потеря канала |

### Архитектурные решения (29 мар)

| # | Решение |
|---|---------|
| А1 | **RLS подтверждён** -- Directus работает поверх RLS-политик |
| А2 | **Access Management -- ORY Keto** (отдельно от Billing) |
| А3 | **Chargeback -- не нужен пока** |
| А4 | **CRM Directus + Metabase -- подтверждён** |

### Архитектурные решения (7 апр, встреча 4)

| # | Решение |
|---|---------|
| Д1 | **Identity: `ory_id` = единственный универсальный ID.** НЕ добавлять `lms_user_id` в Ory traits. Каждый сервис создаёт локальную учётку при первом входе с `ory_id`. Если нужен `lms_user_id` -- через LMS API по Ory-токену |
| Д2 | **ЦД↔LMS при первом входе** (не массовая миграция). Код адаптеров: `bonuses` (`aisistant_client`), `posthog-api` |
| Д3 | **Мягкое удаление:** флаг «заархивировано», история в Git. Не удалять физически |
| Д4 | **Безопасность:** Церен разбирается с практиками сертификаций |

> **Влияние на WP-183:** Решение Д1 подтверждает Р4 (привязка через Ory, не костыль). Identity linking (сценарий 13) теперь имеет чёткий механизм: `ory_id` создаётся при первом входе → `crm.identity_links` связывает `telegram_id ↔ ory_id`. Нет промежуточного маппинга через `lms_user_id`.

### Решения по миграции оплат (7 апр, встреча 5 — команда)

| # | Решение | Обоснование |
|---|---------|-------------|
| М1 | **Q-consolidation: гибридная схема (dual-write + read).** Каналы 1-5: Aisystant пишет в свою PG как сейчас + дублирует каждый платёж/списание в Neon. Каналы 6-8: пишут сразу в Neon (как сейчас). Aisystant получает **read-only доступ к Neon** для проверки оплат (баны, подписки). Данные из Aisystant НЕ удаляются до полной миграции | Дима: «неохота дублировать [в обратном направлении]. Организовать доступ к Neon на чтение для Aisystant. На первом этапе будет проверять: есть ли платёж в текущей базе ИЛИ в Neon» |
| М2 | **Монета — без автоинтеграции пока.** Оплаты вносятся вручную (Гиляна) через **Directus → Neon**. Церен разворачивает Directus + форму ручного ввода. Интеграция с Монетой — отдельная задача | Дима: «Монеты здесь нет. Всё вручную.» Ильшат: интеграцию обсудим следующим шагом |
| М3 | **Биллинг-логика остаётся в Aisystant.** Управление подписками, добавление в стажировки, продление — существующие модули поверх `payment` + `charge_off`. Не переносить сейчас. Церен учитывает это при проектировании схемы Neon (совместимость) | Дима: «В Aisystant есть модуль поверх — после платежа продлевает подписку, добавляет в группу. Он работает, пока не трогаем» |
| М4 | **`charge_off` — основа для принятия решений**, не `payment`. `payment` содержит ВСЕ платежи включая неуспешные. `charge_off` = только успешные списания. Церен реплицирует обе таблицы в Neon, бот Aist и Directus используют `charge_off` для проверки доступов | Дима: «charge_off добавляются только если платёж был успешно проведён. Принимать решение для биллинга нужно на основе списаний» |
| М5 | **Neon API для всех подсистем.** Церен делает API (или прямой PG-доступ), чтобы Aisystant и другие подсистемы могли читать оплаты из Neon | Дима: «Если доступ от сервера бэка до Neon физически есть, пользоваться тем, что есть в Neon, я не вижу проблем» |
| М6 | **Индивидуальные ссылки на оплату с сайта.** API для YooKassa уже есть в Aisystant (подписки, стажировки). Церен интегрирует в новую схему для семинаров на сайте. Для Монеты — добавлять позже | Дима: «API на бэке есть, для трёх платёжных систем уже работает. Фронт делает запрос → бэк взаимодействует с ЮКассой → возвращает индивидуальную ссылку» |
| М7 | **Церен готовит схему в Neon, Дима валидирует.** Дима подключается к Neon через средства разработки, проверяет поля, пишет замечания | Дима: «Как только готова схема в Neon и будет доступ, смогу подключиться и валидировать» |
| М8 | **Деплой Aisystant — вечером.** Монолит, обновлять лучше когда меньше пользователей | Дима: «Не мгновенно, в пределах дня, лучше поздно обновлять» |

> **Ключевое изменение:** Q-consolidation решён НЕ как вариант A (полная миграция) и НЕ как вариант B (два хранилища + Directus). Решение — **гибрид**: Aisystant дублирует в Neon (write), Aisystant читает из Neon (read). Neon постепенно становится единым источником.

### Переходный период: гибридная схема dual-write

> Три состояния системы: AS-IS → ПЕРЕХОДНЫЙ ПЕРИОД → TO-BE.
> Переходный период длится до полного перехода на Neon (Фаза B+). В это время **оба хранилища активны**, но с чёткими ролями.

**AS-IS (сейчас):**
```
Каналы 1-5 → Aisystant PG (единственное хранилище)
Каналы 6-7 → Neon (единственное хранилище)
Канал 8    → нигде (вручную)

Aisystant НЕ видит каналы 6-8.
Бот Aist НЕ видит каналы 1-5 (кроме API подписок с cache 5мин).
```

**ПЕРЕХОДНЫЙ ПЕРИОД (Фаза A):**
```
Каналы 1-5 → Aisystant PG (как сейчас) → dual-write → Neon
Каналы 6-7 → Neon (как сейчас)
Канал 8    → Directus → Neon

Aisystant PG:
  - ПИШЕТ: каналы 1-5 (без изменений, как сейчас)
  - ДУБЛИРУЕТ: каждый save(payment) + save(charge_off) → Neon
  - ЧИТАЕТ: свою PG + Neon (для банов, подписок, доступов)
  - НЕ получает данные из Neon (без обратного дублирования)

Neon:
  - ПИШЕТ: каналы 6-8 (бот + Directus)
  - ПОЛУЧАЕТ: дубликаты каналов 1-5 от Aisystant
  - = единый Реестр оплат (все 8 каналов)

Биллинг-логика Aisystant (подписки, стажировки, продление):
  - Работает на СВОИХ таблицах (Aisystant PG) — без изменений
  - Дополнительно: проверяет Neon на наличие оплат каналов 6-8
  - Решения принимает на основе charge_off (М4)

Directus + Metabase:
  - Работают ТОЛЬКО с Neon
  - Видят все 8 каналов в одном месте

Бот Aist:
  - ПИШЕТ в Neon (каналы 6-7, как сейчас)
  - ЧИТАЕТ из Neon (все каналы)
  - Автодопуск, маршрутка — по данным из Neon
```

**Инварианты переходного периода:**
1. **Aisystant ничего не теряет.** Его PG содержит те же данные, что и раньше. Биллинг работает
2. **Neon = надмножество.** Содержит данные всех 8 каналов (свои + дубликаты из Aisystant)
3. **Дублирование одностороннее:** Aisystant → Neon. НЕ Neon → Aisystant. Дима: «дублировать обратно не хочу, это не нужно»
4. **Read из Neon для Aisystant:** при проверке оплат (баны, подписки) Aisystant проверяет и свою PG, и Neon. Если платёж есть в Neon (каналы 6-8) — он учитывается
5. **Сверка:** cron-задача сравнивает записи Aisystant PG и Neon за последние 24ч. Расхождение = алерт

**Критерии выхода из переходного периода (→ TO-BE):**
1. Все каналы 1-5 стабильно дублируются в Neon (0 расхождений за 2 недели)
2. Aisystant успешно читает из Neon и принимает решения на основе данных оттуда
3. Ory развёрнут (WP-187) → identity linking через `ory_id`, не через `suser_id`
4. Биллинг-логика Aisystant переведена на чтение из Neon (вместо своей PG)

**TO-BE (после выхода из переходного периода):**
```
Все каналы → Neon (единственный источник)
Aisystant = потребитель (read-only из Neon, не пишет в свою PG)
Или: Billing Service (SYS.010) заменяет биллинг-модуль Aisystant
```

</details>

<details open>
<summary><b>0.2. Статус после встречи 5 с командой (7 апр)</b></summary>

### Все утверждённые решения

| Что | Статус | Детали |
|-----|--------|--------|
| CRM = Directus + Metabase | ✅ утверждено (А4, 29 мар) | Directus = окно в данные, не центр. Metabase = дашборды |
| Payment Registry (SYS.011) | ✅ утверждено (Р1-Р3, Г5) | Первый модуль декомпозиции монолита Aisystant |
| Billing Service (SYS.010) | ✅ утверждено (Р2) | Новая система, строится поверх Payment Registry на Фазе B |
| Identity = `ory_id` | ✅ утверждено (Д1, 7 апр) | Единственный ID. Каждый сервис — локальная учётка при первом входе |
| Access Management = Ory Keto | ✅ утверждено (А2, Г1) | Атомарные permissions → роли. Отдельно от Billing |
| RLS | ✅ утверждено (А1) | Directus поверх RLS-политик |
| Мягкое удаление | ✅ утверждено (Д3, 7 апр) | Флаг «заархивировано», не физическое |
| ЦД↔LMS | ✅ утверждено (Д2, 7 апр) | Данные подтягиваются при первом входе, не массовая миграция |
| **Q-consolidation** | ✅ **решено (М1, 7 апр)** | **Гибридная схема dual-write** (не вариант A и не B). Подробнее: §0 «Переходный период» |
| **Монета** | ✅ решено (М2, 7 апр) | Вручную через Directus → Neon. Автоинтеграция — позже |
| **Биллинг-логика** | ✅ решено (М3, 7 апр) | Остаётся в Aisystant, не переносить |
| **charge_off = основа** | ✅ решено (М4, 7 апр) | Для принятия решений — `charge_off`, не `payment` |
| **Инд. ссылки YooKassa** | ✅ решено (М6, 7 апр) | API уже есть на бэке для 3 платёжных систем |

### Открытые вопросы (остались)

**Для Ильшата:**
- Бот МИМ (@SystemsSchool_bot) — как передаёт данные в Neon? Webhook или напрямую? (Р6)
- Интеграция с Монетой (автоматическая) — обсудим следующим шагом

**Для Гиляны:**
- Какие **отчёты** нужны? Ежемесячная выручка, средний доход с клиента, выгрузка за период?
- Нужна ли **сверка** между каналами (выписка ЮКассы vs Реестр оплат)?

**Для Алёны:**
- Какие **разрезы** для маркетинга? По каналам, когортам, продуктам, географии?
- Нужна ли **воронка** (лид → оплата → в чате → активный)?

**Для Юли:**
- Какая информация по **наполняемости групп**? Количество записавшихся, оплативших, активных?
- Преподаватели — нужен ли доступ к спискам участников? Через Directus (RBAC)?

**Для Димы:**
- `telegram_id` при оплате через Tilda/Ecwid — остаётся открытым. Решение через Ory (Фаза B). До Ory: fallback по email

### Дальнейшие действия

> Порядок действий учитывает зависимости. Критический путь: 1 → 2 → 3.
> Параллельно с критическим путём: 4, 5, 6.

| # | Действие | Кто | Dep | Срок | Результат |
|---|----------|-----|-----|------|-----------|
| 1 | **Схема `finance.*` в Neon.** Таблицы `finance.payments` + `finance.charge_offs` на основе структуры Aisystant (18 + 10 колонок). `double → decimal` (округление до 1 руб.). `purpose` как enum string (не ordinal). Добавить `channel` (1-8), `source_system` | Церен | -- | W15 (эта неделя) | Готовая схема. Дима подключается и валидирует (М7) |
| 2 | **Экспорт истории.** Выгрузить все `payment` + `charge_off` из Aisystant PG → миграционный скрипт → загрузить в Neon | Церен + Дима | Действие 1 + доступ к Aisystant PG (Паша) | W15-16 | Исторические данные каналов 1-5 в Neon |
| 3 | **Dual-write: Aisystant → Neon.** После `paymentRepository.save()` и `chargeOffRepository.save()` — запрос в Neon (API или прямой PG-insert). Деплой вечером (М8) | Дима | Действие 1 | W15-16 | Каждый новый платёж/списание каналов 1-5 дублируется в Neon |
| 4 | **Read: Aisystant читает из Neon.** При проверке оплат (баны, подписки) — проверять и свою PG, и Neon | Дима | Действие 1 + доступ к Neon | W16 | Aisystant видит оплаты каналов 6-8 |
| 5 | **Directus на Railway** + Neon datasource + форма ручного ввода (Монета, B2B) | Церен | Действие 1 | W15 (эта неделя) | Гиляна/Алёна/Юля видят все оплаты. Ручной ввод → Neon |
| 6 | **Metabase на Railway** + дашборды (выручка, воронка, наполняемость) | Церен | Действие 5 | W16 | Дашборды для руководства |
| 7 | **Сверка:** cron-задача (24ч) — сравнение Aisystant PG и Neon по каналам 1-5. Алерт при расхождении | Церен | Действие 3 | W16 | Гарантия целостности dual-write |
| 8 | **Автодопуск + маршрутка.** Бот Aist проверяет оплату в Neon → invite / кик | Церен | Действие 3 | W16-17 | Автоматический допуск по всем 8 каналов |
| 9 | **Инд. ссылки на оплату с сайта** (YooKassa). Сайт → API Aisystant → индивидуальная ссылка → оплата → Neon | Церен + сайт | Действие 3 | W17 | Замена общих ссылок на индивидуальные |

**Параллельные треки:**
- **Церен (без dep):** действия 1, 5, 6 — схема Neon, Directus, Metabase
- **Дима (dep: действие 1):** действия 3, 4 — dual-write, read из Neon
- **Совместно:** действие 2 — экспорт истории

</details>

<details>
<summary><b>0.1. Открытые вопросы (обновлено 7 апр)</b></summary>

### Решено (встреча 5, 7 апр)

| # | Вопрос | Решение |
|---|--------|---------|
| ~~1~~ | Q-consolidation | ✅ М1: гибридная схема dual-write |
| ~~2~~ | Webhook при оплате | ✅ М1: Aisystant → Neon напрямую (не webhook в бот) |
| ~~3~~ | Монета/PayAnyWay | ✅ М2: вручную через Directus → Neon |
| ~~17~~ | Ory — модель доступов | ✅ Решено 5 апр: атомарные permissions → роли |

### Остаётся открытым

**Для Димы:**
- `telegram_id` при оплате через Tilda/Ecwid — решение через Ory (Фаза B). До Ory: fallback по email

**Для Ильшата:**
- Бот МИМ (@SystemsSchool_bot) — как передаёт данные в Neon? (Р6)
- Интеграция с Монетой (автоматическая) — следующий шаг

**Для Гиляны:**
- Какие **отчёты** нужны? Ежемесячная выручка, средний доход с клиента, выгрузка?
- Нужна ли **сверка** между каналами?
- Как учитываются оплаты через **Aisystant Corp** (Stripe, USD)?

**Для Алёны:**
- Какие **разрезы** для маркетинга?
- Нужна ли **воронка** (лид → оплата → в чате → активный)?
- **Стоимость привлечения клиента** — откуда данные по расходам?

**Для Юли:**
- **Наполняемость групп** — какая информация нужна?
- **Преподаватели** — нужен ли доступ к спискам через Directus?

**Архитектура (Церен + Андрей):**
- **Баллы (WP-121):** нужна ли `finance.point_transactions` в Neon как placeholder?
- **Revenue Sharing:** считать в Metabase или в Billing Service?
- **Activity Hub (WP-109):** Billing adapter — каждый платёж = `payment_completed`. Когда делать?
- **Permissions:** определить конкретный перечень (карта: функция → permission)

</details>

---

<details open>
<summary><b>1. Проблема: два мира</b></summary>

Сейчас оплаты хранятся в двух несвязанных базах. Бот Aist не знает об оплатах через Tilda/Ecwid/Монету. Бухгалтер и маркетолог не видят оплаты бота Aist. Декан и преподаватели не видят наполняемость групп. Нет единого окна.

```
МИР 1: Aisystant (Java LMS)              МИР 2: Бот Aist (Python)
|- YooKassa (подписки, потоки)            |- YooKassa (семинары витрины)
|- Paybox                                 |- TG Stars (семинары)
|- Stripe (Aisystant Corp)                +- Neon: seminar_payments
|- Монета/PayAnyWay
|- Tilda/Ecwid (витрина)
+- PostgreSQL Aisystant
    (реестр оплат -- де-факто)

         X НЕ СВЯЗАНЫ X
```

**Последствия:**
- Много ручной обработки платежей: оплата через Tilda/Ecwid -> Алёна/Гиляна вручную отправляют invite, добавляют в группы и чаты -> задержка, ошибки
- Учёт в последний момент перед запуском семинаров очень сложный -- данные разбросаны по двум системам, нет единого списка оплативших
- Гиляна не видит оплаты через бот Aist (Stars, ЮКасса семинаров)
- Маршрутка (кик неоплативших) невозможна -- бот Aist не знает всех оплативших
- Юля и преподаватели не видят наполняемость потоков и семинаров в реальном времени
- Нет единой выручки / среднего дохода с клиента -- данные в разных местах

</details>

<details open>
<summary><b>2. Полная карта каналов оплаты (as-is)</b></summary>

### 8 каналов, 3 получателя

| # | Канал | Юрисдикция | Получатель | Обработка (система) | Реестр оплат (документ) | Бот Aist знает? |
|---|-------|-----------|------------|---------------------|------------------------|------------|
| 1 | **YooKassa** (подписки, потоки) | РФ | ИП | Aisystant LMS | Реестр оплат (PostgreSQL Aisystant) | Через API (pull, cache 5мин) |
| 2 | **Paybox** | РФ | ИП | Aisystant LMS | Реестр оплат (PostgreSQL Aisystant) | Нет |
| 3 | **Stripe** | Мир | Aisystant Corp (USA) | Aisystant LMS | Реестр оплат (PostgreSQL Aisystant) | Нет |
| 4 | **Монета/PayAnyWay** | РФ+СНГ+Мир | ИП | Aisystant LMS | Реестр оплат (PostgreSQL Aisystant) | Нет |
| 5 | **Tilda + Ecwid** (витрина -- отдельная страница магазина, на которую ведут с основного сайта) | РФ | ИП | Aisystant LMS | Реестр оплат (PostgreSQL Aisystant) | Нет |
| 6 | **YooKassa** (семинары бота Aist) | РФ | ИП | Бот Aist (Python) | Neon (локальный реестр семинаров) | Да |
| 7 | **TG Stars** (семинары бота Aist) | Глобально | Телеграм (физ.лицо) | Бот Aist (Python) | Neon (локальный реестр семинаров) | Да |
| 8 | **Manual** (B2B, юрлица) | Любая | ИП или Aisystant Corp | Вручную | Нигде (не записывается) | Нет |

> **Примечание:** основной сайт МИМ работает на Aisystant (свой). Tilda+Ecwid -- только витрина (страница магазина), на которую ведёт ссылка с основного сайта.

> **To-be (обновлено 7 апр, М1):** dual-write. Каналы 1-5: Aisystant пишет в свою PG + дублирует в Neon. Каналы 6-7: бот Aist пишет в Neon (как сейчас). Канал 8: Directus -> Neon. Aisystant читает из Neon для проверки оплат (баны, подписки). Neon = единый Реестр оплат.
>
> **Уточнение (М2, М4):** Монета -- вручную через **Directus → Neon** (без автоинтеграции пока). `charge_off` -- основа для принятия решений (только успешные списания), не `payment` (все попытки включая неуспешные).

### 3 получателя платежей

| Получатель | Юрисдикция | Каналы | Валюта |
|------------|-----------|--------|--------|
| ИП (РФ) | Россия | YooKassa, Paybox, Монета, Tilda/Ecwid | RUB |
| Aisystant Corp | США | Stripe | USD/EUR |
| Телеграм (физ.лицо) | Глобально | TG Stars | XTR |

### Что покупают

| Продукт | Каналы оплаты | Тип |
|---------|---------------|-----|
| Подписка БР | YooKassa, Stripe, Paybox, Монета | Рекуррент |
| Поток (интернатура) | YooKassa, Stripe, Paybox, Монета, Tilda/Ecwid | Разовая |
| Семинар (витрина бота Aist) | YooKassa (бот Aist), TG Stars | Разовая |
| Семинар (витрина Tilda/Ecwid) | YooKassa (через Aisystant), Монета, Tilda/Ecwid | Разовая |
| B2B (юрлица) | Manual (счёт + акт) | Разовая |

### Две системы учёта оплат

| Система | ID | Что это | Где сейчас | Куда идёт |
|---------|-----|---------|-----------|-----------|
| **Payment Registry** (система) | SYS.011 | Подсистема платформы: принимает факты оплат из всех каналов, записывает в документ Реестр оплат, отдаёт данные потребителям. **Две таблицы:** `payment` (все попытки оплат, вкл. неуспешные, 18 колонок) и `charge_off` (только успешные списания, 10 колонок). **`charge_off` = основа для биллинг-решений** (М4) | Де-факто существует внутри монолита Aisystant. Реестр оплат = таблицы в PG Aisystant (каналы 1-5) + Neon (каналы 6-7) | Фаза A: dual-write в Neon (М1). Aisystant дублирует + читает из Neon. Фаза B: мигрирует в Billing Service |
| **Биллинг-логика Aisystant** (модуль) | -- | Существующий модуль поверх Payment Registry: после успешного платежа -- продление подписок, добавление в группы стажировок, управление тарифами. **Остаётся в Aisystant** (М3) | Внутри монолита Aisystant, работает на основе `charge_off` | Пока не переносится. Фаза B-C: часть функций мигрирует в Billing Service |
| **Billing Service** (система) | SYS.010 | Новая подсистема: бизнес-логика (тарифы, подписки, баллы, revenue sharing, юнит-экономика, управление доступами) | Не существует как отдельная система | Фаза B-C: строится поверх Payment Registry и Ory |

</details>

<details open>
<summary><b>2.1. Lifecycle семинара</b></summary>

Семинар проходит два режима. Переключение -- вручную или автоматически по дате завершения.

### Режим 1: Live (семинар идёт)

- Многоразовая пригласительная ссылка («маршрутка»)
- Все оплатившие вступают по этой ссылке
- **Накануне семинара (или за пару часов):** бот Aist кикает неоплативших из чата
- После кика -- можно вступить по ссылке только после оплаты (тот же механизм, что при продаже записи: оплата -> одноразовая ссылка)

### Режим 2: Запись (семинар завершился)

- **Отключить** для всех кроме админов возможность добавлять/приглашать новых участников и генерировать вступительные ссылки
- Вступление **только по одноразовой ссылке** (после оплаты)
- Многоразовая ссылка деактивируется

### Переключение режима

| Событие | Действие бота Aist |
|---------|--------------|
| Семинар завершился (дата или ручная команда) | 1. Отозвать многоразовую ссылку. 2. Отключить приглашения для не-админов (`setChatPermissions`). 3. Пометить семинар `mode: recording` |
| Новый семинар запланирован | Создать многоразовую ссылку, включить приглашения, `mode: live` |

### Два варианта допуска (оба рабочие)

| Вариант | Кто | Как | Бот Aist знает? |
|---------|-----|-----|-----------|
| **A. Через бот Aist** | Покупатель оплачивает -> бот Aist генерирует одноразовую ссылку (`createChatInviteLink`, `member_limit=1`) | Автоматически | Да -- бот Aist создал ссылку, видит вступление |
| **B. Через админа** | Админ создаёт одноразовую ссылку через интерфейс Telegram -> отправляет покупателю | Вручную | Да -- бот Aist получает `chat_member` update с `invite_link.creator` = админ. Помечает `source: admin_invite` |

**Предпочтительный вариант: A** (автоматический учёт). Вариант B -- для исключительных случаев (ручной допуск).

</details>

<details open>
<summary><b>2.2. Сценарии оплаты (по Ильшату)</b></summary>

### Сценарий 1: Оплата записей через бот Aist (@aist_me_bot)

```
Покупатель -> бот Aist (сценарий оплаты) -> ссылка на ЮКассу -> оплата
    |
    v
Оплата прошла -> бот Aist записывает в Neon (Реестр оплат)
    -> генерирует одноразовую ссылку -> отправляет покупателю
    |
    v
Покупатель вступает в чат -> бот Aist фиксирует вступление в Neon
    -> связка tg_id с аккаунтом (после Ory -- через Ory)
```

### Сценарий 2: Оплата записей через сайт МИМ («Витрина»)

```
Покупатель выбирает запись на сайте МИМ
    -> вводит: email, имя+фамилия, код семинара, логин в TG (опционально)
    -> сайт запрашивает API Aisystant -> индивидуальная ссылка ЮКассы (М6)
    |
    v
Оплата прошла -> Aisystant записывает в свою PG + dual-write в Neon (М1)
    |
    v
Бот Aist узнаёт о новой оплате в Neon (PG NOTIFY / polling / Directus Flow)
    -> генерирует одноразовую ссылку
    -> ссылка отправляется на email покупателя
    -> бот Aist пробует отправить в личку TG (если покупатель взаимодействовал с ботом ранее)
    |
    v
Покупатель вступает в чат -> бот Aist фиксирует вступление в Neon
```

### Сценарий 3: Оплата семинаров (не записей) с сайта

```
Резидент оплачивает на сайте (ЮКасса) -> указывает email или телефон
    |
    v
AS-IS: Гиляна видит оплату в ЮКассе -> вручную: регистрация в CRM,
    добавление в поток, письмо оплатившему -> резидент вступает в чат
    |
    v
TO-BE: Aisystant записывает оплату + dual-write в Neon (М1)
    -> бот Aist узнаёт из Neon -> генерирует одноразовую ссылку
    -> ссылка отправляется на email
    -> бот Aist пробует отправить в личку TG
```

**Что учесть:**
- Оплатившие не через бот МИМ (@SystemsSchool_bot) не должны быть забанены -- **решено (Р6): оба бота пишут оплаты в Neon**
- При оплате передавать идентификатор потока/код потока (или определять актуальный автоматически)

</details>

<details open>
<summary><b>3. Целевая схема (to-be, с Ory)</b></summary>

**Принципы:**
- **Не дублировать данные** -- каждый source-of-truth остаётся на месте
- **Ory = единый аккаунт** -- один аккаунт связывает Telegram, email и аккаунт Aisystant (заменяет ручную склейку `/link`)
- **Ory = единое управление доступом** -- платформа решает, кто к чему имеет доступ (чаты, функции, контент)

### Единый аккаунт (Ory)

Сейчас один человек может иметь 3 отдельных «аккаунта»: Telegram, email на сайте, Aisystant. Склейка -- вручную через `/link` в боте.

С Ory -- **один аккаунт на человека**:
- Регистрация на сайте -> Ory создаёт аккаунт (email)
- `/start` в боте Aist -> Ory связывает telegram_id с тем же аккаунтом
- Привязка Aisystant -> миграция из LMS в тот же аккаунт

Все каналы оплаты видят одного человека. Нет путаницы «оплатил на сайте, но бот Aist не знает».

### Архитектура (обновлено 7 апр, М1)

```
                         Ory (аккаунты + доступы) [Фаза B]
                                |
                    +-----------+-----------+
                    v           v           v
             Aisystant      Neon PG        Бот Aist
             (монолит)    (Реестр оплат    (Python)
                |          = finance.*)        |
                |               |              |
                +-- dual-write -+   пишет -----+
                    (каналы 1-5)    (каналы 6-8)
                                |
                    +-----------+-----------+
                    v                       v
              Directus                  Metabase
              (CRM UI,                (аналитика)
               ручной ввод)               |
                                          |-> Гиляна: все оплаты, сверка
                                          |-> Алёна: воронка T0->T4
                                          |-> Юля: наполняемость
                                          +-> Преподаватели: списки

              Activity Hub (WP-109)
                          |
                          +-> Billing adapter: платёж = событие
                              -> engagement -> ЦД -> аналитика
```

> **Neon = единый Реестр оплат.** Все потребители (Directus, Metabase, бот Aist, Aisystant) читают из Neon. Aisystant пишет в свою PG + дублирует в Neon (переходный период). Бот Aist и Directus пишут в Neon напрямую.

### Уведомление бота о новых оплатах (Neon → бот Aist)

Когда Aisystant дублирует оплату в Neon (каналы 1-5), бот Aist должен узнать о ней для генерации invite. Варианты механизма (выбрать при реализации):

| Механизм | Задержка | Сложность |
|----------|----------|-----------|
| **PG NOTIFY / LISTEN** | Реалтайм | Neon поддерживает. Бот слушает канал `new_payment` |
| **Directus Flow** (trigger на INSERT → webhook боту) | ~1 сек | Directus Flows, нет retry. Подходит для Фазы A |
| **Polling** (бот проверяет Neon каждые N сек) | До N сек | Простейший вариант. Подходит для начала |

Данные, которые бот Aist читает из Neon при новой оплате:

```sql
SELECT * FROM finance.payments
WHERE channel IN (1,2,3,4,5)  -- каналы от Aisystant
  AND purpose IN ('WORKSHOP', 'SEMINAR', 'INTERNSHIP')
  AND status = 'succeeded'    -- только успешные
  AND notified_bot = false    -- ещё не обработанные
ORDER BY created_at;
```

### Запись вступления в чат (бот Aist → Neon)

Когда человек вступает в чат по одноразовой ссылке, бот Aist записывает факт **напрямую в Neon** (не webhook в Aisystant):

```sql
INSERT INTO finance.chat_joins (telegram_id, chat_id, seminar_code, invite_source, joined_at)
VALUES (123456789, -100123456789, 'SE-2026.2-T', 'bot', NOW());
```

Используется для связки `tg_id` с аккаунтом (после Ory -- через Ory).

### Воронка входа (для маркетинга)

```
T0 (анонимный)
  | /start в боте Aist или заход на сайт
  | -> Ory создаёт аккаунт
  v
T1 (зарегистрирован, без подписки)
  | Trial 30 дней -- полный доступ
  | -> бесплатные семинары, марафон
  v
T2 (подписка БР)
  | Оплата: YooKassa / Stripe / Монета / Stars
  | -> доступ: лента, консультации, заметки, планы, Сообщество IWE
  v
T3 (ЦД подключён)
  | Бесплатно -- заполнить цифровой двойник
  v
T4 (экзокортекс)
  | Бесплатно -- setup.sh / Gateway URL
```

**Metabase считает по Ory:** сколько на каждом тире, конверсия T0->T1->T2, удержание, отток.

### Виды доступов и как они выдаются

| Что получает человек | При каком событии | Как выдаётся сейчас | Как будет с Ory |
|---------------------|-------------------|--------------------|--------------| 
| **Подписка БР** (лента, консультации, заметки) | Оплата подписки | Бот Aist проверяет API Aisystant (cache 5мин) | Ory знает: тир = T2 -> доступ автоматически |
| **Чат Сообщество IWE** | Подписка БР активна | Бот Aist одобряет join request | Ory: есть подписка -> одобрить |
| **Чат семинара** (оплата через бот Aist) | Оплата Stars/ЮКасса | Бот Aist сразу шлёт invite | Без изменений (уже работает) |
| **Чат семинара** (оплата через сайт) | Оплата на сайте | Гиляна/Алёна вручную | Aisystant dual-write в Neon -> бот Aist узнаёт из Neon -> шлёт invite |
| **Видео семинара** | Оплата семинара | Ссылка в сообщении после оплаты | Без изменений |
| **Поток (интернатура)** | Оплата курса | Aisystant LMS выдаёт | Без изменений |
| **Доступ B2B** (юрлица) | Оплата по счёту | Вручную | Менеджер вносит в Directus -> доступ |
| **Кик из чата** (маршрутка, live-режим) | Нет оплаты накануне | Не реализовано | Cron: за 1 день -> бот Aist проверяет Neon (Реестр оплат) -> кикает неоплативших |
| **Кик из чата** (подписка истекла) | Подписка/оплата истекла | Не реализовано | Cron: проверить доступ -> предупреждение -> 3 дня -> кик |

</details>

<details open>
<summary><b>4. Этапы реализации</b></summary>

### Фаза A: Единый учёт + dual-write (можно без Ory)

> **Обновлено после встречи 5 (7 апр).** Ключевое изменение: не webhook, а dual-write. Aisystant дублирует платежи в Neon + читает из Neon. Каналы 6-8 пишут в Neon напрямую.

```
Каналы 1-5 (YooKassa, Paybox, Stripe, Монета, Tilda):
  Webhook -> Aisystant PG (как сейчас) -> dual-write -> Neon

Каналы 6-7 (YooKassa семинары, TG Stars):
  Webhook -> Бот Aist -> Neon (как сейчас)

Канал 8 (Manual, B2B):
  Directus -> Neon

Aisystant (чтение): проверяет платежи в своей PG + в Neon
```

| # | Что | Кто | Dep | Бюджет | Результат |
|---|-----|-----|-----|--------|-----------|
| 0 | ~~**Proposal** -- согласование с командой~~ | Все | -- | 1h | ✅ Встреча 5 (7 апр). Решения М1-М8 |
| 1 | **Схема `finance.*` в Neon** -- таблицы `payments`, `charge_offs` на основе структуры Aisystant (18 + 10 колонок). Округление `double -> decimal` до 1 руб. | Tseren | -- | 3h | Готовая схема, Дима валидирует (М7) |
| 2 | **Dual-write: Aisystant -> Neon.** После `paymentRepository.save()` и `chargeOffRepository.save()` -- запрос в Neon (API или прямой PG-insert). Экспорт истории | Дима + Tseren | Этап 1 | 4h | Все платежи/списания каналов 1-5 дублируются в Neon. Исторические данные мигрированы |
| 3 | **Read: Aisystant читает из Neon.** При проверке оплат (баны, подписки) -- проверять и свою PG, и Neon | Дима | Этап 1 | 2h | Aisystant видит оплаты из всех каналов (1-8), не только из своих (1-5) |
| 4 | **Directus на Railway** + Neon datasource | Tseren | Этап 1 | 2h | Гиляна/Алёна/Юля видят все оплаты через веб-интерфейс Directus |
| 5 | **Metabase на Railway** + дашборды | Tseren | Этап 4 | 3h | Дашборды: ежемесячная выручка, средний доход с клиента, воронка T0->T4, наполняемость |
| 6 | **Автоматический допуск** в чаты (подписчики, семинары) | Tseren | Этап 3 | 2h | Бот Aist проверяет оплату в Neon -> генерирует invite. Подписка БР -> одобрение входа в Сообщество IWE |
| 7 | **Маршрутка** (кик неоплативших из чатов) | Tseren | Этап 3 | 2h | Cron: бот Aist проверяет Neon -> предупреждает -> кикает неоплативших |
| 7.1 | **Lifecycle семинара** (live -> запись) | Tseren | Этап 7 | 1h | Семинар завершился -> режим «только одноразовые ссылки» |
| 8 | **Manual-оплаты** через Directus -> авто-допуск | Tseren | Этап 4 | 1h | B2B: менеджер вносит оплату в Directus -> Neon -> бот Aist -> invite |
| 9 | **Индивидуальные ссылки на оплату с сайта** (YooKassa) | Tseren + сайт | Этап 2 | 2h | Сайт запрашивает API -> бэк создаёт индивидуальную ссылку YooKassa -> пользователь оплачивает -> Aisystant -> Neon (М6) |

**Бюджет Фазы A:** ~22h. Критический путь: Этапы 1-3 (Tseren + Дима). Без Димы: этапы 4, 5, 8 параллельно.

> **Фаза A = начало переходного периода** (см. §0 «Переходный период: гибридная схема dual-write»).
> После Фазы A система находится в переходном состоянии: два хранилища, одностороннее дублирование, Neon = надмножество.
> Выход из переходного периода — после Фазы B (Ory) + стабильный dual-write.

**Что работает после Фазы A:**
- **Переходный период активен:** Aisystant PG + Neon работают параллельно
- Neon = единый Реестр оплат (все 8 каналов)
- Aisystant продолжает работать как сейчас + дублирует в Neon + читает из Neon
- Биллинг-логика Aisystant (подписки, стажировки) — без изменений, на своих таблицах + доп. проверка Neon
- Бот Aist видит все оплаты через Neon (не только свои каналы 6-7)
- Directus + Metabase -- единое окно для руководства
- Ручной ввод (Монета, B2B) — через Directus → Neon
- Допуск в чаты автоматический (по данным из Neon)
- Маршрутка работает по всем каналам
- Индивидуальные ссылки на оплату с сайта (YooKassa)
- Сверка 24ч: cron сравнивает Aisystant PG и Neon, алерт при расхождении

### Фаза B: Единый аккаунт через Ory (после WP-187)

| # | Что | Кто | Dep | Бюджет | Результат |
|---|-----|-----|-----|--------|-----------|
| B1 | **Управление доступами через Ory** -- подписки, чаты, функции | Tseren + Дима | WP-187 (Ory развёрнут) | 4h | Одно место для всех доступов: Ory решает, кто к чему имеет доступ (чаты, подписки, функции) |
| B2 | **Миграция аккаунтов** -- связка telegram + email + aisystant в один аккаунт | Tseren + Дима | B1 | 2h | Один человек = один аккаунт Ory. Нет дубликатов, нет ручной склейки `/link` |
| B3 | **Бот Aist проверяет доступы через Ory** (вместо отдельных запросов к API Aisystant и таблицам Neon) | Tseren | B1 | 3h | Бот Aist спрашивает только Ory «есть ли доступ?» вместо обращения в 2 разные системы |
| B4 | **Единый вход в Directus** через Ory | Tseren | B1 | 1h | Гиляна/Алёна/Юля логинятся в Directus через тот же аккаунт Ory |
| B5 | **Выход из переходного периода.** Биллинг-логика Aisystant переключается на чтение из Neon (вместо своей PG). Dual-write можно отключить. Neon = единственный источник | Дима + Tseren | B1-B3 + 0 расхождений 2 нед | 4h | Переходный период завершён |

**Бюджет Фазы B:** ~14h. Dep: WP-187.

**Что меняется после Фазы B:**
- Нет ручной склейки `/link` -- аккаунты связаны автоматически
- Бот Aist не спрашивает разные системы «есть ли подписка?» -- спрашивает Ory
- Оплата на сайте -> Ory знает -> бот Aist знает
- **Переходный период завершён** (B5): Neon = единственный источник, dual-write отключен

### Фаза C: Экономика (после Фаз A+B)

| # | Что | Кто | Dep | Бюджет | Результат |
|---|-----|-----|-----|--------|-----------|
| C1 | **Баллы** (Points Engine, WP-121) | Tseren | Фаза B | 6h | Начисление баллов за активность, списание за семинары/курсы. 1 балл = 1 руб. Альтернативный способ оплаты |
| C2 | **Revenue Sharing** | Tseren | Фаза A | 3h | Автоматическое распределение дохода: платформа 30% / автор 50% / инструктор 15% / куратор 5% |
| C3 | **Юнит-экономика в Metabase** | Tseren | Фаза A | 2h | Дашборды: стоимость привлечения клиента, средний доход с клиента, отток по каналам и продуктам |

**Бюджет Фазы C:** ~11h.

</details>

<details>
<summary><b>5. Связь с другими РП</b></summary>

| РП | Как связано |
|-----|------------|
| **WP-73** | §2.9 Биллинг -- обновить: SYS.010 Billing Service + SYS.011 Payment Registry |
| **WP-74** | SC-12 -- дополнить: витрина бота Aist, deep links, маршрутка |
| **WP-109** | Activity Hub -- Billing adapter: каждый платёж = событие |
| **WP-115** | Полный E2E сценарий семинара: маркетинг -> оплата (бот Aist/сайт) -> авто-допуск в чат (3-5 сек) -> маршрутка (кик неоплативших) -> видео (JWT Kinescope). Реализация витрины (showcase.py) покрывает оплату+допуск; остаётся: маршрутка, JWT-видео, HMAC в deep links |
| **WP-181** | Воронка чатов -- бот Aist читает Neon (Реестр оплат) для авто-invite |
| **WP-183** | CRM+Billing -- этот proposal = расширение WP-183 |
| **WP-188** | Маркетинг -- Metabase дашборды |
| **WP-194** | Подписка БР -- ценообразование |
| **WP-121** | Points Engine -- баллы как альтернативная оплата, начисление за активность |
| **WP-187** | Ory (Kratos + Keto) -- единый identity + access control. Фаза B зависит от WP-187 |
| **WP-198** | Бот-вышибала -- маршрутка: cron-кик |

</details>

---

*Создано: 2026-04-03. Обновлено: 2026-04-07 (встреча 5 — решения М1-М8, dual-write, переходный период, обновление всех секций на Neon = Реестр оплат).*
